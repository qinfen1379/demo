#!/usr/bin/env groovy
pipeline {
    agent any 
        stages {
            stage('Build') {
                steps {
                    sh 'mkdir /home/testdemo'
                    echo 'hello jenkins'
                }
            }
        }
}
