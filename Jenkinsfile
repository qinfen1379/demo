#!/usr/bin/env groovy
pipeline {
    agent any 
        stages {
            stage('Build') {
                steps {
                    echo 'do not git push'
                    echo 'hello jenkins'
                }
            }
        }
}
