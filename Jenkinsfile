#!/usr/bin/env groovy
pipeline {
    agent any 
        stages {
            stage('Build') {
                steps {
                    def username = 'hello'
                    echo "my val is ${username}"
                    echo 'hello jenkins test'
                }
            }
        }
}
