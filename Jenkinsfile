#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
				def username = "qinfen"
				echo "my name is ${username}"
				echo "Running ${env.BUILD_ID}"
            }
        }
    }
}
